# Project Blueprint

This document outlines the architecture and structure of the project.

## Static Assets

All static assets, such as images, are stored in the `public` directory. The `images` folder within the `public` directory (`public/images`) is the designated location for all image files.